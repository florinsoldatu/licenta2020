spring.application.name=Help Me Cook

endpoints.restart.enabled=true
server.servlet.path=/*

# set spring.cloud.config.failFast=false (or remove the line)
# if you want the client to fall back to the local config when the remote config server is down
spring.cloud.config.failFast=false